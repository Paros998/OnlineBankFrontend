import React, {FC} from 'react';
interface PriorityOrdersProps{
  className?:string;

}

const PriorityOrders:FC<PriorityOrdersProps> = ({className}) => {
  return (
    <div className={className}>
      
    </div>
  );
};

export default PriorityOrders;